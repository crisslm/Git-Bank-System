 ArrayList<CurrentAccount> users = new ArrayList<>();

        Login newLogin = new Login("Hannamail", "12345");
        CurrentAccount newAccount = new CurrentAccount(1111, 0, "Hanna", "12/12/2025", newLogin);

        //This works, PERFECT!!!
        users.add(newAccount);

        CurrentAccount actual = findByAccountNumber(users, 1111);
        System.out.println(actual.getLogin().getPassword());

        echo "# Git-Bank-System" >> README.md
git init
git add README.md
git commit -m "Middle Project"
git branch -M main
git remote add origin https://github.com/crisslm/Git-Bank-System.git
git push -u origin main